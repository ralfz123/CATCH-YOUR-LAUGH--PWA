<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ('../partials/head') %>
    <title>Home | CATCH YOUR LAUGH</title>
  </head>
  <body>
    <%- include ('../partials/header') %>

    <main>
      <section id="combo-generator">
        <div class="loader"></div>

        <% dataCat.forEach(item => { %>
        <article id="cat-images">
          <img id="image-cat" src="<%= item.url %>" alt="Image of a cat" />
        </article>
        <% }) %>

        <article id="jokes">
          <h3 id="joke"><%= dataJokes.setup %></h3>
          <h3 id="punchline"><%= dataJokes.punchline %></h3>
        </article>

        <button id="comboBtn">Another combo!</button>

        <article id="like-area">
          <h2>Like this combo!</h2>
          <button id="likeBtn">❤️</button>
          <a href="favourites">My Favourites</a>
        </article>
      </section>
    </main>

    <%- include ('../partials/footer') %>
  </body>
</html>
