// Acces My Favourites only when favourites.array.lenght > 0

export function checkFavsBtn(favouritesArray) {
  // When favouritesArray.length > 0, set button to enabled
  if (favouritesArray.length > 0) {
    // Set button to enabled
    //   href="/favourites"
    //   cursor = "pointer"
  }
  

  // After using preferred method to target link
  // link.addEventListener('click', function (event) {
  //     if (this.parentElement.classList.contains('isDisabled')) {
  //       event.preventDefault();
  //     }
  //   });
}
